<%= link_to reservations_path do %>
    <h1> HOTech Risort </h1>
<% end %>

<%= link_to t('.add'), new_reservation_path if Current.user%>
<%= link_to t('.rooms'), rooms_path if Current.user&.admin? %>
<%= link_to t('.login'), new_session_path unless Current.user %>
<%= link_to t('.register'), new_user_path if Current.user&.admin?  %>
<%= link_to t('.logout'), session_path(Current.user.id), data: {
    turbo_method: :delete,
    turbo_confirm: t('authentication.sessions.msgs.confirm')
} if Current.user %>